# Constrix Mobile - React Native App

## 🚀 Quick Start

### 1. Install Dependencies
```bash
cd constrix-mobile
npm install
```

### 2. Install Expo CLI
```bash
npm install -g @expo/cli
```

### 3. Start Development Server
```bash
npm start
# or
npx expo start
```

### 4. Preview on Your Phone

**Option A: Expo Go App (Recommended)**
1. Install "Expo Go" app on your phone (iOS/Android)
2. Scan the QR code that appears in terminal
3. App will load instantly on your phone!

**Option B: Simulator**
- Press `i` for iOS simulator (Mac only)
- Press `a` for Android emulator

## 📱 Features

- ✅ Wepin wallet integration
- ✅ Clean UI matching web app
- ✅ TypeScript support
- ✅ Camera integration ready
- ✅ Push notifications ready

## 📁 Project Structure

```
src/
├── components/     # Reusable UI components
├── screens/        # App screens
├── services/       # Wepin, API services
└── types/          # TypeScript types
```

## 🚀 Next Steps

1. **Test on phone** with Expo Go
2. **Add navigation** between screens
3. **Implement habit tracking**
4. **Add camera for proof photos**
5. **Integrate push notifications**

The app is ready to run! Just scan the QR code with Expo Go.